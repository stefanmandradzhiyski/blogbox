package com.snmi.enums;

/**
 * NewsFilter enum must be used when you want to determinate
 * by what criteria the received user information will be filtered
 * @author Stefan Mandradzhiyski
 * @version 1.0
 */
public enum UserFilter {
    FIRST_NAME,
    USERNAME,
    REGISTERED
}
